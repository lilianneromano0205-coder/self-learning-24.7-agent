{
  "name": "expert-fleet-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker control plane for Expert Fleet: a Durable Object alarm that wakes the container to drain its queue, and a REST sandbox for sandbox.py",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7",
    "@cloudflare/sandbox": "^0.12.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260825.1",
    "typescript": "^5.9.0",
    "wrangler": "^4.125.0"
  }
}
